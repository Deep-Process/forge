# /review $ARGUMENTS

Deep structured code review for a task (optional — basic verification is built into `/next` Step 5).

Use `/review` for critical tasks that need a thorough multi-perspective audit beyond the automatic verification in `/next`. For routine tasks, the built-in deep-verify step in `/next` is sufficient.

Read and follow the procedure in `skills/review/SKILL.md`.

The task to review is: $ARGUMENTS
